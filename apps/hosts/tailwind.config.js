const config = {
    theme: {
        extend: {
            colors: {
                primary: '#FF5733',  // A bold primary color
                secondary: '#FF00FF',  // Outstanding secondary color (bright magenta)
                accent: '#00FFFF',  // Cyan for accent
            },
        },
    },
    plugins: [],
}

export default config;
